
<div class="h-screen w-full flex flex-col justify-center items-center">
  <div class="max-w-xl w-full flex flex-col">

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field mb-4">
          <%= f.label 'Correo' %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => 'w-full rounded-md' %>
        </div>

        <div class="field mb-4">
          <%= f.label 'Contraseña' %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> caracteres minimo)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password", :class => 'w-full rounded-md' %>
        </div>

        <div class="field mb-4">
          <%= f.label 'Confirmar Contraseña' %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", :class => 'w-full rounded-md' %>
        </div>

        <div class="actions">
          <%= f.submit "Crear Usuario", :class => 'bg-indigo-500 text-white w-full py-4 rounded-md cursor-pointer hover:bg-indigo-700' %>
        </div>
      <% end %>
  </div>
</div>
  