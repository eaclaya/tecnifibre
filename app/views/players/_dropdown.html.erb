<div class="flex flex-col bg-white shadow-lg rounded-md">
    <% players.each do |player| %>
        <div class="px-4 py-2 hover:bg-gray-200 cursor-pointer" onclick="addPlayer(<%= player.id %>)"><%= player.name %></div>    
    <% end %>
</div>