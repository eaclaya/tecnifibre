<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl my-8">Torneos</h1>
    <% if user_signed_in?%>
        <%= link_to 'Nuevo Torneo', new_tour_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
    <% end %>
  </div>

  <div id="tours" class="min-w-full grid grid-cols-1 md:grid-cols-3 items-end gap-4">
    <%= render @tours %>
  </div>
</div>
